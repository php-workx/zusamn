/**
 * Placeholder stubs for app flows. Replace with Firestore calls.
 */
export async function seedExampleList(): Promise<void> {
  return Promise.resolve();
}

export async function acceptInviteLink(_token: string): Promise<void> {
  void _token;
  return Promise.resolve();
}
