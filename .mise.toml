[env]

[tools]
# Java 21 required locally for Firebase Emulator Suite (firebase-tools@15+)
# EAS Build uses Java 17 by default - the Android build.gradle handles this via
# sourceCompatibility/targetCompatibility settings, so local dev uses 21 while
# EAS Build continues to work with its bundled Java 17.
java = "21"
node = "22"
